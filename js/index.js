// Declaring Variables
var exampleOne = 'This is old school, probably should not be used now.';
let exampleTwo = 'Let can be updated, but not redeclared within the same scope';
const exampleThree = `Const can't be updated or redeclared`; 
// More information @ https://www.freecodecamp.org/news/var-let-and-const-whats-the-difference/

let exampleArray = ['Arrays are like lists...', 'they hold different types of data']

let exampleObject = {
  key: 'value',
  typeInt: 5,
  typeString: 'string',
  typeArray: [1, 2, 3, 4, 5],
  typeObject: {
    object: 'inside an object',
    array: ['Array as a value of a key inside an object, that is inside an object'],
  }
};
